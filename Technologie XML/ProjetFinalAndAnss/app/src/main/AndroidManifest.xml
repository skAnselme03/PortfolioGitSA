<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetFinalAndAnss"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:label="Accueil"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VueAdmin"
            android:label="Vue administrateur - Gestion des usagers">
        </activity>
        <activity
            android:name=".VueSecurity"
            android:label="Vue sécurité">
        </activity>
        <activity
            android:name=".VueVisitor"
            android:label="Vue utilisateur">
        </activity>
        <activity
            android:name=".AjoutUsager"
            android:label="Ajout d'un usager par un administrateur">
        </activity>
        <activity
            android:name=".ModifierUsager"
            android:label="Modification d'un usager par un administrateur">
        </activity>
    </application>

</manifest>